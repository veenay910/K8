apiVersion: v1
kind: Service

metadata:
  name: nodeport-service
  namespace: roboshop

spec:
  type: NodePort
  selector:
    proj: roboshop
    comp: frontend
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetport: 80

    
